# Venice API Configuration (Required)
VENICE_API_KEY=your-venice-api-key-here

# Server Configuration (Optional)
PORT=3000

# CORS Configuration (Optional)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://signal.venice.guru

# Environment (Optional)
NODE_ENV=development

# Debug Logging (Optional)
# Set to "true" to enable verbose API request/response logging for debugging
DEBUG_API_REQUESTS=false

# Security Configuration (Optional)
# Dashboard API Key - if set, all dashboard endpoints require X-API-Key header
# Recommended: Use a strong random string (32+ characters)
# Generate with: openssl rand -hex 32
# Leave empty to disable authentication (not recommended for production)
DASHBOARD_API_KEY=

# Logging Configuration (Optional)
# Set to "json" for structured JSON logs (useful for log aggregation systems)
# Set to "text" for human-readable logs (default)
LOG_FORMAT=text
